<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pneumax.mobilesystembygolaung">
    <!-- VERY IMPORTANT! Don't forget this permission, or in-app billing won't work. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="com.google.socialapp.permission.SHARE_POST" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_user_password"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/ic_user_password"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileStoreSystemNoActionBar">
        <activity android:name=".Summary.SummaryHandHeldOperate"></activity>
        <!-- <activity android:name=".activity.SplashScreenActivity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <!-- android:theme="@style/Theme.splashScreenTheme"> -->
        <!-- android:theme="@style/Theme.MobileStoreSystemNoActionBar"> -->

        <activity android:name=".activity.SplashActivity"></activity>
        <activity android:name=".SummaryActivity" />
        <activity android:name=".Summary.SummaryRFCheckOutActivity" />
        <activity android:name=".Summary.SummaryRFCheckInActivity" />
        <activity android:name=".activity.StoringParttubeActivity" />
        <activity android:name=".activity.StoreDoctypeActivity" />
        <activity android:name=".activity.ReceiveDoctypeActivity" />
        <activity android:name=".activity.ReceivePackingListActivity" />
        <activity android:name=".activity.StoringActivity" />
        <activity android:name=".activity.ReceivingActivity" />
        <activity android:name=".activity.PickingJobtubeActivity" />
        <activity android:name=".activity.PickingActivity" />
        <activity android:name=".activity.ClearDocumentError" />
        <activity android:name=".activity.PhysicalCountActivity" />
        <activity android:name=".activity.DeliveryActivity" />
        <activity android:name=".activity.ConfirmActivity" />
        <activity android:name=".activity.CheckPartActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>